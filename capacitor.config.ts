import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dameheart.shortsradar',
  appName: 'shortsradarpro',
  webDir: 'dist'
};

export default config;
